# Crestpoint Modular V2 Test Checkpoints

This document defines the required test checkpoints for every phase and module.

---

# Required Phase Checkpoint

After every phase, run:

```bash
npm run build
git status